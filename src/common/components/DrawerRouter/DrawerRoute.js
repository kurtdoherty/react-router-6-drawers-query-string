/**
 * This provides us with some syntactic sugar by allowing drawer route definitions to be
 * defined in JSX instead of an array of objects. We would have TS prop types associated with
 * this component to describe the expected interface.
 */
function DrawerRoute({ element, id, path, size }) {
  return null;
}

export default DrawerRoute;
